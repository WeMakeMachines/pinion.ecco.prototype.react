export enum Units {
  IMPERIAL = "imperial",
  METRIC = "metric",
}

export enum TemperatureUnits {
  CELSIUS = "celsius",
  FAHRENHEIT = "fahrenheit",
}

export enum IconStyle {
  FILLED = "fill",
  LINE = "line",
}
